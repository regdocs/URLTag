{
  "name": "url-tag",
  "version": "1.0.0",
  "description": "Custom tag analytics for the web (generate tags and track in batches)",
  "main": "index.ts",
  "scripts": {
    "start:dev": "npx nodemon",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js"
  },
  "author": "Jayanta Pandit <jay.dnb@outlook.in>",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.15.3",
    "nodemon": "^2.0.21",
    "rimraf": "^4.4.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongodb": "^5.1.0"
  }
}
